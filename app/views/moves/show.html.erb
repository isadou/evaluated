<div class="container-image-centered">
  <%= image_tag("Background_top.svg", :class => "background-top")%>
</div>

  <div class="text-pages-top">
      <h1 class="title-white title-order show-title">Tout sur votre déménagement</h1>
  </div>

<div class="container-show">
  <div class="details-show">
    <p><%= @volume_total %> m3</p>
    <%= link_to 'Voir plus', recap_move_path(@move), class: '' %>
    <p>Transport : <%= @move.transport %></p>
    <p> <%= @cartons %> cartons</p>
    <p> Effectif : <%= @effectif_total %> </p>
    <%= link_to 'Voir plus',  details_move_path(@move), class: '' %>
  </div>

  <div id="map"
  style="width: 100%; height: 350px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

  <div id="instructions">
  </div>
</div>
